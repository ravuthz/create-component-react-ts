import React, { useEffect, useState, useCallback } from 'react';

export const use{{pascalCase name}} = (props: any) => {
  const [count, setCount] = useState(0);

  const increase = useCallback(() => setCount((x) => x + 1), []);

  const decrease = useCallback(() => setCount((x) => x - 1), []);

  useEffect(() => {
    console.log('{{pascalCase name}} mounted');

    return () => {
      console.log('{{pascalCase name}} unmounted');
    }
  }, []);

  return {count, setCount, increase, decrease};
};
